1. DISPLAY NUMBER OF EEMPLOYEEE, DEPARTMENT WISE
2. DISPLAY LIST OF ALL CONSTRAINTS ON EMP TABLE
3. DISPLAY JOB WISE NUMBER OF EMPLOYEE WHERE NUMBER OF EMPLOYEE IS GREATER THAN 2
4. DISPLAY ENAME, DEPTNO, DEPTNAME(DEPT TABLE) 
5. DISPLAY NAME OF EMPLOYEE WITH MANAGER NAME

6.CREATE A TABLE CEMP(EMPNO,ENAME,SAL)
7.ADD PRIMARY KEY ON EMPNO AND UNIQUE KEY ON ENAME
8.  REMOVE UNIQUE KEY FROM ENAME
9.DISPLAY BOTTOM 5 SALARY
DISPLAY 10TH HIGHEST SALARY
DISPLAY NAME OF EMPLOY WHOSE COMMISSION IS NULL


1. SELECT DEPTNO, COUNT(*) FROM EMP GROUP BY DEPTNO;
2.SELECT CONSTRAINT_NAME FROM USER_CONSTRAINTS WHERE TABLE_NAME = 'EMP';
3. SELECT JOB, COUNT(*) FROM EMP HAVING COUNT(EMPNO)>2 GROUP BY JOB;
4. SELECT E.ENAME,E.DEPTNO, D.DNAME FROM EMP E, DEPT D
   WHERE D.DEPTNO = E.DEPTNO;
5.SELECT E.ENAME AS MANAGER, D.ENAME AS ENAME FROM EMP E, EMP D
  WHERE E.EMPNO = D.MGR;
6.CREATE TABLE CEMP
  (EMPNO NUMBER(4) ,
   ENAME VARCHAR2(20),
   SAL NUMBER(6));
7.ALTER TABLE CEMP ADD CONSTRAINT PK_C PRIMARY KEY(EMPNO);
  ALTER TABLE CEMP ADD CONSTRAINT UK_C UNIQUE(ENAME);
8 ALTER TABLE CEMP DROP CONSTRAINT PK_C;
  ALTER TABLE CEMP DROP CONSTRAINT UK_C;
9.SELECT SAL FROM EMP WHERE ROWNUM <6 ORDER BY SAL;
10.
11. SELECT ENAME FROM EMP WHERE NOT(COMM IS NOT NULL);

HOW TO DISPLAY UNIQUE ROWS IN ORACLE SQL

DISTINCT UNIQUE GROUP BY , UNION OPERATOR WORKS TOO

UNION OPERATOR ORDERS BY ASCENDING ORDER 

